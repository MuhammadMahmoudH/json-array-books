# This is a sample Python script.

# Press Shift+F10 to execute it or replace it with your code.
# Press Double Shift to search everywhere for classes, files, tool windows, actions, and settings.


def print_hi(name):
    # Use a breakpoint in the code line below to debug your script.
    print(f'Hi, {name}')  # Press Ctrl+F8 to toggle the breakpoint.


# Press the green button in the gutter to run the script.
if __name__ == '__main__':
    print_hi('PyCharm')

# See PyCharm help at https://www.jetbrains.com/help/pycharm/


# temp

# print(output)

# # Extract the desired columns
# for row in data:
#     print(row)

# column1 = [row[0] for row in data]
# column2 = [row[0][1] for row in data]

# Create a new dictionary with the prompt and response keys
# new_data = {'prompt': column1, 'response': column2}
#
# # Save the new JSON file
# with open('data/after/output.json', 'w', encoding='utf-8') as file:
#     json.dump(new_data, file)

# with open('input.json', 'r', encoding='utf-8') as f:
#     data = json.load(f)

# output = {}
# for section, content in data.items():
#     output[section] = {
#         'text': content['text'],
#         'footnote': content['footnote']
#     }
#

# for key, content in data, data.items():
#     print(key, content)
    # output[section] = {
    #     'text': content['text'],
    #     'footnote': content['footnote']
    # }